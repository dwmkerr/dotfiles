{
  //  I never like my editor to format on save and perfer it to be a manual task
  //  or a hook. Ths disables auto-format on save for all file types (which also
  //  makes sure we don't get hands on saving).
  //  "coc.preferences.formatOnSaveFiletypes": [],

  //  I've had tonnes of problems with Jedi for Python. Instead, use the MPLS
  //  which is the Microsoft Python Language Server. See:
  //    https://github.com/neoclide/coc-python#get-start
  "python.jediEnabled": false,

  //  I love the python hints but they're weird in vim as it looks like you've
  //  entered them yourself, so disable for now.
  "pyright.inlayHints.functionReturnTypes": false,
  "pyright.inlayHints.variableTypes": false,
  "pyright.inlayHints.parameterTypes": false
}
